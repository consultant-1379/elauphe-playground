pipeline {
    agent {
        node {
            label params.SLAVE
        }
    }
    stages {
        stage('Testing') {
            steps {
                sh "ls"
            }
        }
    }
}
